/** Shared control classes for the detail edit/verify/note forms (matches the resume flow's look). */
export const inputClass =
  "w-full rounded-md border border-black/15 px-2.5 py-1.5 text-sm focus:ring-2 focus:ring-navy focus:outline-none disabled:opacity-50";

export const selectClass = inputClass;
